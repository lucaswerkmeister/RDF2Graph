SELECT ?pred
FROM <%2$s>
WHERE
{
  { SELECT (COUNT(?subj) AS ?totalCount) WHERE { ?subj wdt:P31 <%1$s> . } }
  ?subj wdt:P31 <%1$s> .
  ?subj ?pred ?target .
}
GROUP BY ?pred ?totalCount
HAVING(COUNT(DISTINCT ?subj) >= ?totalCount/10)
