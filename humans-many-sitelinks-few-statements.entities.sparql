SELECT ?entity WHERE {
  ?entity wdt:P31 wd:Q5;
          wikibase:sitelinks ?sitelinks. hint:Prior hint:rangeSafe true.
  ?entity wikibase:statements ?statements. hint:Prior hint:rangeSafe true.
  FILTER(?sitelinks > 75)
  FILTER(?statements < 50)
}
ORDER BY ASC(?statements)
LIMIT 50
