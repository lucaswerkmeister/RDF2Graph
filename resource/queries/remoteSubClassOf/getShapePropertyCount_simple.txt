SELECT (COUNT(*) as ?count)
FROM <%4$s>
WHERE
{
  {
    ?subj wdt:P31 <%1$s> .
  }
  UNION
  {
    ?subj wdt:P279+ <%1$s> .
  }
  ?subj <%2$s> ?target .
  BIND(iri(DATATYPE(?target)) as ?dtype)
  FILTER(bound(?dtype) && ?dtype = <%3$s>)
}
